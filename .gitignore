# ------------------------------
# Node / JavaScript (existing)
# ------------------------------
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.*
!.env.example

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
# public   # Uncomment if using Gatsby (and not Next.js)

# vuepress build output
.vuepress/dist

# vuepress v2.x temp and cache directory
.temp
.cache

# Sveltekit cache directory
.svelte-kit/

# vitepress build output
**/.vitepress/dist

# vitepress cache directory
**/.vitepress/cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# Firebase cache directory
.firebase/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v3
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vite logs files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# ------------------------------
# Vue / Vite specific additions
# ------------------------------
# Vite cache (sometimes created in project root)
.vite/

# Vue CLI build outputs (already covered by /dist, but keep explicit)
# dist/

# Storybook (for Vue)
storybook-static/
.out/

# Cypress / Playwright artifacts (common in Vue apps)
cypress/videos/
cypress/screenshots/
cypress/downloads/
playwright-report/
test-results/
.playwright/

# Local dev tools
*.local

# ------------------------------
# Editor / OS miscellany
# ------------------------------
# VS Code workspace (keep common project files if you want)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# JetBrains / Rider
.idea/
*.iml

# macOS / Windows
.DS_Store
Thumbs.db

# ------------------------------
# C# / .NET (Visual Studio / Rider)
# ------------------------------
# Build results
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# Visual Studio
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.rsuser

# Test results & coverage
[Tt]est[Rr]esult*/
[Tt]est[Rr]esults/
*.coverage
*.coveragexml

# NuGet
*.nupkg
*.snupkg
# packages/ is used in older projects (packages.config). If you still use it, uncomment:
# packages/
# except build/ within packages (keep targets/props if needed)
# !packages/*/build/
# !packages/*/build/**

# MSBuild artifacts
*.cache
*.obj
*.pdb
*.ipch
*.iobj
*.pch
*.ilk

# Publish outputs
app.publish/
[Aa]rtifacts/
publish/
# Rider default publish dir
**/bin/Release/net*/publish/

# Entity Framework
migrations/obj/

# Azure Functions / local secrets
local.settings.json

# ASP.NET Scaffolding
ScaffoldingReadMe.txt

# StyleCop / Sonar / analyzers
StyleCopReport.xml
.sonarqube/
.scannerwork/

# ReSharper / Rider caches
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user

# Tye / Service Fabric / Containers (optional)
.tye/

# User-specific files (common SDK tooling)
*.vc.db
*.vscode.sqlite

# ASP.NET IIS Express
IISExpress.config

# .NET Core generated files
project.lock.json
project.fragment.lock.json

# Application settings (keep your base appsettings.json in source; ignore local overrides)
appsettings.*.local.json
App.config.user
Web.config.user

# ------------------------------
# Docker (optional, if present)
# ------------------------------
# Uncomment if you generate these locally and don’t want them committed
# docker-compose.override.yml
# .docker/
# **/.dockerignore
